%h1 Gespielte Legs

%table
  %tr
    %th Datum
    %th
    %th
    %th

  - @legs.each do |leg|
    %tr
      %td= leg.created_at.strftime("%d %b %y")
      %td= link_to image_tag('icon_lupe.gif'), leg
      %td= link_to image_tag('trash-icon-16x16.gif'), leg, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Neues Leg beginnen', { :controller => "legs", :action => "create" }, :method => "post"
