= form_for @leg do |f|
  - if @leg.errors.any?
    #error_explanation
      %h2= "#{pluralize(@leg.errors.count, "error")} prohibited this leg from being saved:"
      %ul
        - @leg.errors.full_messages.each do |msg|
          %li= msg

  %dt Verbleibende Punktzahl:
  .remaining-leg-score#leg-score{ data: { id: @leg.id } }
    %dt= @leg.score

  %dt Erzielte Punktzahl:
  .current-score#current-score 0

  %a.nextthrow{ href: '#' } Nächster Wurf

  .actions
    = render partial: 'score_fields'
